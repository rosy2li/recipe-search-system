# V47 最终总结可找回的 SQL 说明

本目录用于保留 V47 阶段数据库初始化成果，供最终总结/答辩/重新导入数据库时使用。

- `00_full_import_recipe_V47_fix1.sql`：完整导入脚本，包含建库、建表、插入全部数据。
- `01_schema_V47_fix1.sql`：仅数据库结构脚本，只建库建表，不插入数据。

V48 及以后默认不再把完整菜谱数据作为前端静态文件携带，系统运行时直接读取 MySQL。
